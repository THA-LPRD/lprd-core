{
  "name": "lprd-core-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "format": "turbo run format"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.12",
    "oxlint": "^1.60.0",
    "turbo": "^2.9.6"
  },
  "packageManager": "bun@1.3.10",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
